extends TileMap

onready var tile_map_partner : TileMap = $"../TileMap"

#func _ready() -> void:
	#covers every cell that exists with a dark tile
#	for each_cell in tile_map_partner.get_used_cells():
#		print(each_cell)
#		set_cellv(each_cell,0)
